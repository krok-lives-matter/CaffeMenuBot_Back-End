# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_nuke --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: nuke

on:
  push:
    branches:
      - main

jobs:
  ubuntu-18_04:
    name: ubuntu-18_04
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd Test'
        run: ./build.cmd Test
        env:
            GitHubToken: ${{ secrets.GITHUB_TOKEN }}
  macOS-10_14:
    name: macOS-10_14
    runs-on: macOS-10.14
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd Test'
        run: ./build.cmd Test
        env:
            GitHubToken: ${{ secrets.GITHUB_TOKEN }}
  windows-2019:
    name: windows-2019
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd Test'
        run: ./build.cmd Test
        env:
            GitHubToken: ${{ secrets.GITHUB_TOKEN }}
